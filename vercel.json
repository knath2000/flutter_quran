{
  "version": 2,
  "builds": [
    {
      "src": ".",
      "use": "@vercel/static-build",
      "config": {
        "installCommand": "apt-get update && apt-get install -y curl xz-utils && curl -L -o flutter.tar.xz https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.27.0-stable.tar.xz && mkdir /flutter && tar xf flutter.tar.xz -C / && rm flutter.tar.xz && export PATH=\"$PATH:/flutter/bin\" && flutter config --enable-web && flutter pub get",
        "buildCommand": "export PATH=\"$PATH:/flutter/bin\" && flutter build web --release",
        "outputDirectory": "build/web"
      }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}